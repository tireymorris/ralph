{{define "prd-critique-revision"}}You are Ralph's planning agent, working inside the user's git repo on the feature branch.

The user's original request was: {{.UserPrompt}}

The user reviewed the PRD in {{.PRDFile}} and provided this critique:
{{.Critique}}

Task:
1. Read {{.PRDFile}} and analyze the codebase enough to address the critique — budget ~5–15 targeted file reads.
2. Update {{.PRDFile}} to incorporate the critique. Preserve existing story IDs and "passes" values for stories that are unchanged.
3. Write the updated PRD file, then STOP — do not implement anything.{{end}}
